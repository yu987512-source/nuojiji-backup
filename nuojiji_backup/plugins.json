[{"id":"mini-player","name":"悬浮播放器","version":"1.1.1","author":"糯叽机内置示例","description":"可拖拽的悬浮音乐播放器。点「＋」选一个本地音频文件或贴一个音频网址即可添加曲目。注意：App 切到后台时 WebView 会挂起音频，这是 WebView 的限制。","entry":{"html":"<div id=\"wrap\"><div id=\"title\">—</div><div id=\"bar\"><i id=\"prog\"></i></div><div id=\"row\"><button id=\"prev\" title=\"上一首\">⏮</button><button id=\"play\" title=\"播放\">▶</button><button id=\"next\" title=\"下一首\">⏭</button><span id=\"time\">0:00</span><button id=\"add\" title=\"添加曲目\">＋</button><button id=\"del\" title=\"移除当前\">🗑</button></div><div id=\"msg\"></div></div>","css":"html,body{background:transparent}#wrap{padding:8px 10px;display:flex;flex-direction:column;gap:6px;font-size:12px;color:inherit}#title{font-weight:600;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;opacity:.9}#bar{height:4px;border-radius:2px;background:rgba(127,127,127,.25);overflow:hidden;cursor:pointer}#prog{display:block;height:100%;width:0;background:linear-gradient(90deg,#7fb7ff,#4a8fe7);border-radius:2px}#row{display:flex;align-items:center;gap:7px}#row button{background:none;border:none;cursor:pointer;font-size:15px;line-height:1;padding:2px 3px;color:inherit;opacity:.85}#row button:disabled{opacity:.28;cursor:default}#time{margin-left:auto;font-variant-numeric:tabular-nums;opacity:.6;font-size:11px}#add,#del{font-size:13px!important}#msg{font-size:10.5px;line-height:1.4;opacity:.6;min-height:1em}","js":"// 悬浮播放器示例\n//\n// 数据通路：宿主建的 blob: URL 在 null origin 的 iframe 里取不到，\n// 所以音频以 ArrayBuffer 过桥，由插件在【自己的 origin】里 createObjectURL\n// 之后再喂给 <audio>。CSP 的 media-src 允许 blob: 正是为了这条路。\n//\n// 添加曲目走 nuo.media.pick()：由【宿主】开选择器、抓字节、存起来，只把 id 回给插件。\n// 插件全程碰不到 URL —— 这是支持网址的唯一安全做法（放宽 CSP 会开出外连通道）。\n\nvar el = {};\n['title','prog','bar','play','prev','next','time','msg','add','del'].forEach(function(k){ el[k] = document.getElementById(k); });\n\nvar audio = new Audio();\nvar tracks = [];        // [{id, name}]\nvar idx = 0;\nvar urlCache = {};\nvar cfg = { volume: 80, loop: false };\n\nfunction fmt(s){ if(!isFinite(s)||s<0) s=0; var m=Math.floor(s/60), r=Math.floor(s%60); return m+':'+(r<10?'0':'')+r; }\nfunction say(t){ el.msg.textContent = t || ''; }\nfunction setPlayIcon(){ el.play.textContent = audio.paused ? '▶' : '⏸'; }\n\nfunction sync(){\n  var n = tracks.length;\n  el.play.disabled = n === 0;\n  el.prev.disabled = n < 2;\n  el.next.disabled = n < 2;\n  el.del.disabled = n === 0;\n  if (!n) { el.title.textContent = '—'; say('点 ＋ 添加曲目'); }\n  else { el.title.textContent = tracks[idx] ? (tracks[idx].name || ('#' + tracks[idx].id)) : '—'; }\n}\n\nfunction persist(){ return nuo.storage.set('tracks', JSON.stringify(tracks)); }\n\nasync function urlFor(t){\n  if (urlCache[t.id]) return urlCache[t.id];\n  var buf;\n  try { buf = await nuo.media.read(t.id); }\n  catch (e) { nuo.log('media.read 失败 ' + t.id + ': ' + (e && e.message)); return null; }\n  if (!buf || !buf.byteLength) return null;\n  var url = nuo.media.toObjectUrl(buf, 'audio/mpeg');\n  urlCache[t.id] = url;\n  return url;\n}\n\nasync function load(i, autoplay){\n  if (!tracks.length) { sync(); return; }\n  idx = (i + tracks.length) % tracks.length;\n  var t = tracks[idx];\n  el.title.textContent = t.name || ('#' + t.id);\n  say('载入中…');\n  var url = await urlFor(t);\n  if (!url) { say('这个文件读不到了'); return; }\n  audio.src = url;\n  audio.volume = Math.max(0, Math.min(1, cfg.volume / 100));\n  say('');\n  if (autoplay) {\n    // 自动播放策略：只有用户手势触发的那一次才会成功。被拒时不报错，提示用户点一下。\n    audio.play().then(setPlayIcon).catch(function(){ say('点播放键开始（浏览器要求手势）'); setPlayIcon(); });\n  }\n  setPlayIcon();\n}\n\nel.play.addEventListener('click', function(){\n  if (!tracks.length) return;\n  if (!audio.src) { load(idx, true); return; }\n  if (audio.paused) audio.play().then(setPlayIcon).catch(function(e){ say('播放被拒: ' + (e && e.name)); });\n  else { audio.pause(); setPlayIcon(); }\n});\n\nel.next.addEventListener('click', function(){ load(idx + 1, true); });\nel.prev.addEventListener('click', function(){ load(idx - 1, true); });\n\nel.add.addEventListener('click', async function(){\n  say('选择文件或粘贴网址…');\n  var picked;\n  try { picked = await nuo.media.pick('audio'); }\n  catch (e) { say('添加失败'); nuo.log(String(e && e.message)); return; }\n  if (!picked || !picked.id) { say(tracks.length ? '' : '点 ＋ 添加曲目'); return; }\n  tracks.push({ id: picked.id, name: picked.name || ('#' + picked.id) });\n  await persist();\n  say(tracks.length + ' 首');\n  // sync() 必须先跑：load() 不管按钮状态，只走 load 的话播放键会一直是灰的\n  sync();\n  // 第一首自动开播；后续只加进列表不打断当前播放\n  if (tracks.length === 1) load(0, true);\n});\n\nel.del.addEventListener('click', async function(){\n  if (!tracks.length) return;\n  var gone = tracks.splice(idx, 1)[0];\n  if (gone) delete urlCache[gone.id];\n  await persist();\n  audio.pause(); audio.src = '';\n  if (tracks.length) { load(idx, false); } else { sync(); setPlayIcon(); }\n});\n\nel.bar.addEventListener('click', function(ev){\n  if (!audio.duration || !isFinite(audio.duration)) return;\n  var r = el.bar.getBoundingClientRect();\n  audio.currentTime = ((ev.clientX - r.left) / r.width) * audio.duration;\n});\n\naudio.addEventListener('timeupdate', function(){\n  if (audio.duration && isFinite(audio.duration)) el.prog.style.width = ((audio.currentTime / audio.duration) * 100) + '%';\n  el.time.textContent = fmt(audio.currentTime);\n});\n\naudio.addEventListener('ended', function(){\n  if (cfg.loop) { audio.currentTime = 0; audio.play().catch(function(){}); }\n  else if (tracks.length > 1) load(idx + 1, true);\n  else setPlayIcon();\n});\n\naudio.addEventListener('error', function(){ say('这个文件解不了码'); });\n\nasync function init(){\n  var s = await nuo.storage.getAll();\n  if (s && typeof s === 'object') {\n    if (s.volume) cfg.volume = parseInt(s.volume, 10) || 80;\n    cfg.loop = !!s.loop;\n    if (s.tracks) {\n      try { var arr = JSON.parse(s.tracks); if (Array.isArray(arr)) tracks = arr.filter(function(t){ return t && t.id != null; }); }\n      catch (e) { nuo.log('曲目列表损坏，已重置'); }\n    }\n  }\n  audio.volume = Math.max(0, Math.min(1, cfg.volume / 100));\n  sync();\n  setPlayIcon();\n  if (tracks.length) say(tracks.length + ' 首');\n}\n\ninit().then(function(){ nuo.ready(); }).catch(function(e){ say('初始化失败'); nuo.log(String(e && e.message)); nuo.ready(); });\n"},"slots":[{"type":"floating","title":"播放器","icon":"","width":268,"height":150,"defaultOpen":true}],"permissions":["storage","media.read"],"net":{"domains":[]},"userFields":[{"key":"volume","type":"number","label":"音量 0-100","default":"80"},{"key":"loop","type":"toggle","label":"单曲循环","default":""}],"imageKeys":[],"enabled":true,"order":0,"installedAt":1787629017369,"updatedAt":1787629017369},{"id":"status-card","name":"角色状态卡","version":"1.2.0","author":"糯叽机内置示例","description":"在每条角色消息下方显示一张状态卡（好感度 / 心情 / 自定义变量）。数值来自聊天变量 —— 去「聊天设定 → 聊天变量」里加一个「好感度」试试，或让世界书的 {{setvar}} / 角色回复里的 <var> 标记自动驱动它。","entry":{"html":"<div id=\"card\" class=\"hidden\"><div class=\"row\" id=\"r1\"><span class=\"lbl\" id=\"l1\"></span><div class=\"bar\"><i id=\"b1\"></i></div><span class=\"val\" id=\"v1\"></span></div><div class=\"row\" id=\"r2\"><span class=\"lbl\" id=\"l2\"></span><span class=\"txt\" id=\"v2\"></span></div></div><div id=\"hint\" class=\"hidden\"></div>","css":".hidden{display:none!important}#card{padding:5px 10px;border-radius:9px;background:rgba(127,127,127,.10);font-size:11px;line-height:1.5;display:flex;flex-direction:column;gap:2px}.row{display:flex;align-items:center;gap:6px}.lbl{opacity:.6;flex-shrink:0;min-width:3.5em}.val{opacity:.85;font-variant-numeric:tabular-nums;flex-shrink:0}.txt{opacity:.85;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.bar{flex:1;height:5px;border-radius:3px;background:rgba(127,127,127,.22);overflow:hidden;min-width:40px}.bar i{display:block;height:100%;width:0;border-radius:3px;background:linear-gradient(90deg,#ff9f9f,#ff6b8a);transition:width .35s ease}#hint{padding:4px 10px;font-size:10.5px;line-height:1.45;opacity:.45;border-radius:8px;border:1px dashed rgba(127,127,127,.3)}","js":"// 装饰器插件必须写成「收到数据就重绘」——宿主会回收复用 iframe，\n// 靠 on('render') 换数据而不是重建文档（见 pluginSrcDoc.js 说明）。\nvar cfg = { var1: '好感度', var2: '心情', barMax: 100, showOnUser: false, everyMsg: false };\n// 最後一次收到的訊息 —— 變量改了要重繪時沒有它就不知道該畫什麼\nvar lastMsg = null;\nvar el = {\n  card: document.getElementById('card'), hint: document.getElementById('hint'),\n  r1: document.getElementById('r1'), r2: document.getElementById('r2'),\n  l1: document.getElementById('l1'), v1: document.getElementById('v1'), b1: document.getElementById('b1'),\n  l2: document.getElementById('l2'), v2: document.getElementById('v2')\n};\n\nfunction fit() { nuo.ui.resize(document.body.scrollHeight); }\n\nfunction hideAll() {\n  el.card.className = 'hidden';\n  el.hint.className = 'hidden';\n  nuo.ui.resize(0);\n}\n\n// 一个变量都没有时，只在【最新一条】下面留一行提示。\n// 完全隐藏的话，用户分不清「插件在跑但没数据」和「插件根本没装上」——\n// 示例插件尤其不能让人有这种疑惑。挂在每条消息下面又太吵，所以只挂最后一条。\nfunction showHint() {\n  el.card.className = 'hidden';\n  el.hint.className = '';\n  el.hint.textContent = '状态卡已启用：去「聊天设定 → 聊天变量」加一个「' + cfg.var1 + '」就会显示在这里';\n  fit();\n}\n\nasync function loadCfg() {\n  var s = await nuo.storage.getAll();\n  if (s && typeof s === 'object') {\n    if (s.var1) cfg.var1 = s.var1;\n    if (s.var2) cfg.var2 = s.var2;\n    if (s.barMax) cfg.barMax = parseInt(s.barMax, 10) || 100;\n    cfg.showOnUser = !!s.showOnUser;\n    cfg.everyMsg = !!s.everyMsg;\n  }\n}\n\nasync function render(msg) {\n  if (!msg) { hideAll(); return; }\n  // 變量只有「當前值」一個，掛在每條歷史訊息下面就是同一個數字重複幾十遍 ——\n  // 又吵又占地方（而且每條都是一個 iframe）。預設只挂最新一條。\n  if (!cfg.everyMsg && !msg.isLast) { hideAll(); return; }\n  if (msg.role === 'user' && !cfg.showOnUser) { hideAll(); return; }\n\n  var bag;\n  try { bag = await nuo.vars.getAll(); } catch (e) { hideAll(); return; }\n  var local = (bag && bag.local) || {};\n\n  var raw1 = local[cfg.var1];\n  var raw2 = local[cfg.var2];\n\n  if (raw1 === undefined && raw2 === undefined) {\n    if (msg.isLast) showHint(); else hideAll();\n    return;\n  }\n\n  el.hint.className = 'hidden';\n  el.card.className = '';\n\n  if (raw1 === undefined) {\n    el.r1.style.display = 'none';\n  } else {\n    el.r1.style.display = '';\n    var n = parseFloat(raw1);\n    if (isNaN(n)) {\n      // 非数值就当文字显示，不硬套进度条\n      el.l1.textContent = cfg.var1; el.v1.textContent = String(raw1); el.b1.style.width = '0';\n    } else {\n      var pct = Math.max(0, Math.min(100, (n / cfg.barMax) * 100));\n      el.l1.textContent = cfg.var1;\n      el.v1.textContent = n + ' / ' + cfg.barMax;\n      el.b1.style.width = pct + '%';\n    }\n  }\n\n  if (raw2 === undefined) {\n    el.r2.style.display = 'none';\n  } else {\n    el.r2.style.display = '';\n    el.l2.textContent = cfg.var2;\n    el.v2.textContent = String(raw2);\n  }\n\n  fit();\n}\n\nnuo.on('render', function (msg) { lastMsg = msg; render(msg); });\n\n// 變量被改了（AI 的 <var> 標記 / 用戶在「聊天設定 → 聊天變量」手改 / 觸發器）。\n// 沒有這一條的話，卡片只在【訊息變動】時才重讀 —— 用戶改完數值盯著進度條\n// 一動不動，只能退出重進，看起來就像壞了。\nnuo.on('vars', function () { if (lastMsg) render(lastMsg); });\n\nloadCfg().then(function () { nuo.ready(); }).catch(function () { nuo.ready(); });\n"},"slots":[{"type":"messageDecorator","title":"","icon":"","width":0,"height":46,"defaultOpen":false}],"permissions":["storage","vars.read"],"net":{"domains":[]},"userFields":[{"key":"var1","type":"text","label":"变量1名称","default":"好感度"},{"key":"var2","type":"text","label":"变量2名称","default":"心情"},{"key":"barMax","type":"number","label":"进度条满值","default":"100"},{"key":"showOnUser","type":"toggle","label":"用户消息也显示","default":""},{"key":"everyMsg","type":"toggle","label":"每条消息都显示（默认只挂最新一条）","default":""}],"imageKeys":[],"enabled":true,"order":1,"installedAt":1787629078581,"updatedAt":1787718340287}]